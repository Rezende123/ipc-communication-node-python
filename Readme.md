# Comunicação ipc node-python

Duas aplicações exemplo de como pode se dar a comunicação entre um servidor socket python e um cliente node via IPC

### Start Client  
`npm run client`

### Start Server  
`npm run server`